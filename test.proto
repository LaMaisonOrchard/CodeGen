

// This is a comment

object(proto, enum);
object(proto, message);
object(message, message);

include "fred" ;

here = uint32_t;

there = 345.8 ;

enum billbo
{
   TYPE = uint8_t;
   fred = 4;
   biil = 1;
}

message General
{
	ID = 9;
	
	message SET
	{
		ID = 8;
		billbo fred = 4;
		here biil = 1;
	}
}
