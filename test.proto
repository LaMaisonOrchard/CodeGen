

// This is a comment

object(proto, enum);
object(proto, typeDefn);
object(proto, message);
object(message, message);
type(enum);
type(typeDefn);

include "fred" ;

name = test;
here = uint32_t;

there = 345.8 ;

typeDefn here
{
   DESC = "Raw type";
}

enum billbo
{
   TYPE = uint8_t;
   DESC = "ENUM type";
   fred = 4;
   bill = 1;
}

message General
{
	ID = 9;
	
	message SET
	{
		ID = 8;
		billbo fred = 4;
		here biil = 1;
	}
}
